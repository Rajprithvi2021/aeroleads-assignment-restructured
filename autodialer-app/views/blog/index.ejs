<!DOCTYPE html>
<html>
<head>
  <title>Blog</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
</head>
<body>
  <nav class="navbar navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="/">Autodialer</a>
      <a class="btn btn-primary" href="/blog/create">Create</a>
    </div>
  </nav>
  <div class="container my-5">
    <h1 class="mb-4">Blog Posts</h1>
    <div class="row">
      <% if (blogs && blogs.length > 0) { blogs.forEach(function(blog){ %>
      <div class="col-md-6 mb-4">
        <div class="card h-100">
          <div class="card-body">
            <h5 class="card-title"><%= blog.title %></h5>
            <p class="card-text text-muted"><%= blog.excerpt %></p>
          </div>
          <div class="d-flex justify-content-between small px-3 pb-2">
            <span><%= new Date(blog.createdAt).toLocaleDateString() %></span>
            <a href="/blog/<%= blog.slug %>" class="btn btn-sm btn-primary">Read</a>
          </div>
        </div>
      </div>
      <% }); } else { %>
        <p class="text-center">No posts found.</p>
      <% } %>
    </div>
  </div>
</body>
</html>
